Os templates de agentes deste diretório foram copiados da tag `v0.2.0`, de
`templates/engine/.claude/agents/`. Os testes os renderizam e simulam os blocos e metadados do
add-on legado, incluindo customizações locais. O cenário 0.4.0 acrescenta os papéis stock que
essa versão cria ao preservar adapters controlados por add-on.
